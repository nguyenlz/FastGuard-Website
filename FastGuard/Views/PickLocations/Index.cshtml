@model IEnumerable<FastGuard.Data.PickLocation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PickLocationName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PickLocationName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location.LocationId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PickLocationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PickLocationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PickLocationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
